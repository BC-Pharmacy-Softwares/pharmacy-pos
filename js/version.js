/* ============================================================
   version.js — single source of truth for the app version.
   Bump APP_VERSION on every new build, and keep
   electron-app/package.json "version" in sync (it names the .exe).

   Scheme:  MAJOR.MINOR.PATCH
     MAJOR  — big changes / breaking
     MINOR  — new features
     PATCH  — fixes / small tweaks (most builds)
   ============================================================ */
window.APP_VERSION = '1.4.1';
